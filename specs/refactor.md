# Completely refactor the package

## Tasks

* Completely refactor the package to match the specification in .agents/project.md
* Move the macros in the src/macros to `unitopia-macros`
* Refactor the drafts in src/drafts into their own packages (still workspace members)
* Move the quantities in src/quantities to `unitopia-measure`
* Move the units in src/units.rs into `unitopia-marker-units`
